import { RechercheFoyerPipe } from './recherche-foyer.pipe';

describe('RechercheFoyerPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheFoyerPipe();
    expect(pipe).toBeTruthy();
  });
});
